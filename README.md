Install help for Fedora on Windows Subsystem for Linux
======================================================

```
$ sudo apt-get update
$ sudo apt-get install -y wget git
$ wget [location of the script] | bash
```


